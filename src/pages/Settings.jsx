import { useMemo, useState } from 'react';
import { UserPlus, Trash2, Shield, LogOut } from 'lucide-react';
import { getUsers } from './Login';
const KEY='recon_control_users';
const save=u=>localStorage.setItem(KEY,JSON.stringify(u));
export default function Settings({ session, onLogout }) {
  const isAdmin = session?.role === 'Admin';
  const [users,setUsers]=useState(getUsers()); const [form,setForm]=useState({username:'',password:'',name:'',role:'Staff'}); const [msg,setMsg]=useState('');
  const add=e=>{e.preventDefault(); if(!form.username||!form.password){setMsg('Username dan password wajib diisi.');return;} if(users.some(u=>u.username===form.username)){setMsg('Username sudah digunakan.');return;} const next=[...users,{...form,name:form.name||form.username}]; save(next); setUsers(next); setForm({username:'',password:'',name:'',role:'Staff'}); setMsg('Akun berhasil dibuat.');};
  const remove=username=>{if(username==='admin'){setMsg('Akun admin utama tidak dapat dihapus.');return;} const next=users.filter(u=>u.username!==username);save(next);setUsers(next);};
  return <div className="space-y-5"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-soft dark:border-white/10 dark:bg-[#212743]"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[.18em] text-[#7088A9]">Pengaturan</p><h2 className="mt-1 text-xl font-black dark:text-white">Manajemen Akun</h2><p className="mt-1 text-sm text-slate-500">Kelola akses dashboard. Pembuatan dan penghapusan akun hanya tersedia untuk Admin.</p></div><button onClick={onLogout} className="inline-flex items-center gap-2 rounded-lg bg-slate-100 px-3 py-2 text-xs font-black dark:bg-white/5 dark:text-white"><LogOut size={14}/> Logout</button></div></section>{isAdmin ? <div className="grid gap-5 xl:grid-cols-[.8fr_1.2fr]"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-soft dark:border-white/10 dark:bg-[#212743]"><div className="flex items-center gap-2"><UserPlus size={18}/><h3 className="font-black dark:text-white">Tambah Akun</h3></div><form onSubmit={add} className="mt-4 space-y-3"><input className="field w-full" placeholder="Nama" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="field w-full" placeholder="Username" value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/><input className="field w-full" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><select className="field w-full" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>Staff</option><option>Admin</option></select>{msg&&<p className="rounded-lg bg-slate-50 p-3 text-xs font-bold text-slate-600 dark:bg-white/5 dark:text-slate-300">{msg}</p>}<button className="w-full rounded-xl bg-[#1473E6] py-3 text-sm font-black text-white">Buat Akun</button></form></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-soft dark:border-white/10 dark:bg-[#212743]"><div className="flex items-center gap-2"><Shield size={18}/><h3 className="font-black dark:text-white">Daftar Akun</h3></div><div className="mt-4 overflow-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left text-xs uppercase text-slate-400"><th className="px-3 py-3">Nama</th><th className="px-3 py-3">Username</th><th className="px-3 py-3">Role</th><th className="px-3 py-3">Aksi</th></tr></thead><tbody>{users.map(u=><tr key={u.username} className="border-b border-slate-100 dark:border-white/5"><td className="px-3 py-3 font-bold">{u.name}</td><td className="px-3 py-3">{u.username}</td><td className="px-3 py-3">{u.role}</td><td className="px-3 py-3">{u.username!=='admin'&&<button onClick={()=>remove(u.username)} className="text-rose-600"><Trash2 size={16}/></button>}</td></tr>)}</tbody></table></div></section></div> : <section className="rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-900 dark:border-amber-400/20 dark:bg-amber-400/10 dark:text-amber-100"><p className="font-black">Akses terbatas</p><p className="mt-1">Akun Staff dapat menggunakan dashboard, Daily Check, dan Reconciliation, tetapi hanya Admin yang dapat membuat atau menghapus akun.</p></section>}</div>;
}
